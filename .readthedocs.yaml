# Read the Docs configuration file
version: 2

build:
  os: ubuntu-22.04  # Fixes the container crash by matching Python 3.10
  tools:
    python: "3.10"

sphinx:
   configuration: ./docs/source/conf.py
   fail_on_warning: false
  
python:
   install:
   - requirements: requirements.txt